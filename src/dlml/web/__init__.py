"""The dlml Streamlit web app -- the ``dlml explorer`` app.

This subpackage is the packaged Streamlit application (entry point ``app.py``,
launched by ``dlml explorer``). It is intentionally excluded from the project's
ruff, mypy, codespell, and coverage gates (see ``pyproject.toml``); bringing it
up to dlml-core's standards is a tracked follow-up.
"""
